# Dependencies
node_modules/
.pnp
.pnp.js

# Builds
dist/
build/
.next/
out/
.turbo/

# Env
.env
.env.local
.env.*.local
backend/.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# TypeScript build info
*.tsbuildinfo
next-env.d.ts

# Original font drop (we copied into packages/theme/fonts)
/fonts/

# Local Claude Code workspace config
.claude/

# Local server logs
/tmp/*-dev.log

# Python (backend/)
backend/__pycache__/
backend/.venv/
backend/.pytest_cache/
backend/*.pyc
# Patient-data scratch produced by FastAPI when running locally
backend/report_*.json
backend/symptom_check_*.json
backend/med_log_*.txt

# Python (root + nested)
__pycache__/
*.pyc
.pytest_cache/

# Fine-tune artifacts (too big for git; see METHODS.md to regenerate)
# Downloaded GGUF files (~4GB total)
Unsloth/
# Build logs + resumable scratch
data/build.log
data/build.err
data/build_checkpoint.jsonl
# Kaggle datasets (fetched at build time, not committed)
data/mtsamples.csv
data/lab_test_results_public.csv
data/metadata.json
# HuggingFace / training output
outputs/
# Local server stdout/stderr (backend + Next.js dev)
.logs/

# Claude Code / agent internal workspace
.agents/
.scratch/
.superpowers/
.mcp.json
skills-lock.json
